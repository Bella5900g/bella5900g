name: Daily Commits

on:
  schedule:
    - cron: '0 9 * * *'  # Runs daily at 9 AM
  workflow_dispatch:

jobs:
  daily-commits:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Configure Git
        run: |
          git config --local user.email "bellacandy5900g@gmail.com"
          git config --local user.name "Bella5900g"

      - name: Daily activity
        run: |
          # Create daily commits
          echo "Daily activity - $(date)" >> daily-log.txt
          git add daily-log.txt
          git commit -m "📅 Daily activity - $(date +%Y-%m-%d)" || true
          
          git push origin main || true
